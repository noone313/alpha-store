<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= categoryInfo.categoryName %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@300;400;500;700&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Tajawal', sans-serif;
        }

        :root {
            --primary: #2D4263;
            --secondary: #C84B31;
            --light-bg: #F8F9FA;
            --dark-text: #2C3E50;
        }

        body {
            background-color: var(--light-bg);
        }

        .container {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 1rem;
        }

        .category-title {
            font-size: 2rem;
            color: var(--primary);
            text-align: center;
            margin-bottom: 2rem;
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 2rem;
            padding: 1rem;
        }

        .product-card {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 16px rgba(0,0,0,0.05);
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 24px rgba(0,0,0,0.1);
        }

        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-bottom: 2px solid var(--primary);
        }

        .product-info {
            padding: 1.5rem;
            text-align: center;
        }

        .product-name {
            font-size: 1.2rem;
            color: var(--primary);
            margin-bottom: 1rem;
        }

        .product-category {
            font-size: 1rem;
            color: var(--secondary);
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .products-grid {
                grid-template-columns: 1fr;
            }

            
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container">
        <h1 class="category-title"><%= categoryInfo.categoryName %></h1>

        <% if (oneCategory.length === 0) { %>
            <p style="text-align: center; color: var(--dark-text);">لا توجد منتجات في هذه الفئة حالياً</p>
        <% } else { %>
            <div class="products-grid">
                <% oneCategory.forEach(product => { %>
                    <div class="product-card">
                        <img src="/<%= product.productImage %>" class="product-image" alt="<%= product.productName %>">
                        <div class="product-info">
                            <h3 class="product-name"><%= product.productName %></h3>
                            <span class="product-category"><%= product.Category.categoryName %></span>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>